# Nexus Chain

A Go web service built with Gin framework.

## Getting Started

To run the service:

```bash
go run cmd/nexus-chain/main.go
```

The service will start on `http://localhost:8080`.

## API

- GET /ping: Returns a pong message.
